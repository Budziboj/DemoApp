<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <% farm.name %></title>
</head>

<body>
    <h1>Edit <%= farm.name %></h1>
    <form action="/farms/<%=farm._id%>?_method=PUT" method="POST">
        <label for="name">Farm Name</label>
        <input type="text" name="name" id="name" placeholder="<%= farm.name %>" value="<%= farm.name %>">
        <label for="city">City</label>
        <input type="number" id="price" name="price" placeholder="<%= farm.city %>" value="<%= farm.city %>">
        <label for="email">Email</label>
        <input type="email" name="email" id="email" placeholder="<%= farm.email %>">
        <button>Submit</button>
    </form>

    <a href="/farms/<%= farm._id %>">Cancel</a>

</body>

</html>